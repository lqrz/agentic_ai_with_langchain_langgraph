[project]
name = "agentic-ai-with-langchain-langgraph"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.2.7",
]

[dependency-groups]
dev = [
    "jupyterlab>=4.5.3",
]
